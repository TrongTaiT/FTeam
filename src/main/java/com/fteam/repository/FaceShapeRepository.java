package com.fteam.repository;

import com.fteam.model.FaceShape;

import org.springframework.data.jpa.repository.JpaRepository;

public interface FaceShapeRepository extends JpaRepository<FaceShape, Integer>{

}
